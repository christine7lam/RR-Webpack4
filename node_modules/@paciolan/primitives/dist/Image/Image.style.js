"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _utilities = require("@paciolan/utilities");

var styleGenerator = _utilities.componentUtils.styleGenerator;


var style = {
  base: function base(props) {
    return "\n    width: 100%;\n    height: " + (props.height ? props.height + "px" : "initial") + ";\n    " + (props.fill ? "\n      background-image: " + (props.src ? "url(" + props.src + ")" : "none") + ";\n      background-size: " + (props.fill === "cover" ? "cover" : props.fill === "scale" ? "contain" : "") + ";\n      background-color: transparent;\n      background-position: center;\n      background-repeat: no-repeat;\n    " : "\n      // <img> styles here\n    ") + "\n  ";
  }
};

var HeadingStyle = styleGenerator(style);

exports.default = HeadingStyle;