"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.columnStyle = exports.rowStyle = exports.layoutStyle = undefined;

var _utilities = require("@paciolan/utilities");

var styleGenerator = _utilities.componentUtils.styleGenerator;


var row = {
  base: function base(props) {
    var height = props.height,
        fold = props.fold,
        size = props.size,
        align = props.align,
        verticalAlign = props.verticalAlign;

    var isFolded = fold && size.width < fold;
    var defaultJustify = align || "normal";
    var foldedJustify = verticalAlign === "bottom" ? "flex-end" : verticalAlign || "normal";
    return "\n      display: flex;\n      // height: " + (height ? height + "px" : "initial") + ";\n      flex: " + (height === "fill" ? "1 0 auto" : height ? "0 0 " + height + "px" : "0 0 auto") + ";\n      width: 100%;\n      flex-direction: " + (isFolded ? "column" : "row") + ";\n      justify-content: " + (isFolded ? foldedJustify : defaultJustify) + ";\n    ";
  }
};

var column = {
  base: function base(props) {
    var verticalAlign = props.verticalAlign,
        size = props.size,
        width = props.width,
        isFolded = props.isFolded,
        isHidden = props.isHidden,
        gutter = props.gutter,
        always = props.always,
        atLeast = props.atLeast,
        atMost = props.atMost,
        _props$textAlign = props.textAlign,
        textAlign = _props$textAlign === undefined ? "initial" : _props$textAlign;

    var setFlex = size || 1;
    switch (true) {
      case isFolded === true:
        setFlex = 0;
        break;
      case typeof always !== "undefined":
        setFlex = "0 0 " + always + "px";
        break;
      case typeof atLeast !== "undefined":
        setFlex = "1 0 " + atLeast + "px";
        break;
      case typeof atMost !== "undefined":
        setFlex = "0 1 " + atMost + "px";
        break;
      case width > 0:
        setFlex = "0 0 " + width + "px";
        break;
    }
    var setAlign = "initial";
    if (verticalAlign === "bottom") {
      setAlign = "flex-end";
    } else if (verticalAlign === "center") {
      setAlign = "center";
    }
    return "\n      flex: " + setFlex + ";\n      display: " + (isHidden ? "none" : "flex") + ";\n      flex-direction: column;\n      overflow: hidden;\n      margin-right: " + (gutter || 0) + "px;\n      justify-content: " + setAlign + ";\n      text-align: " + textAlign + ";\n    ";
  }
};

var layout = {
  base: function base(props) {
    return "\n    display: flex;\n    flex-direction: column;\n    min-height: " + (props.page ? "100vh" : "initial") + ";\n  ";
  }
};

var layoutStyle = exports.layoutStyle = styleGenerator(layout);
var rowStyle = exports.rowStyle = styleGenerator(row);
var columnStyle = exports.columnStyle = styleGenerator(column);