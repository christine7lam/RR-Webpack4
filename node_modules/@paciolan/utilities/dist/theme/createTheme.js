"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _styles = require("@material-ui/core/styles");

var themes = {
  operator: {
    context: "operator",
    blue: "#3f7be1",
    darkBlue: "#2b669a",
    white: "#fff"
  },
  consumer: {
    context: "consumer"
  }
};

var createTheme = function createTheme(context) {
  if (!(context in themes)) {
    console.warn("Context '" + context + "' is not recognized as a valid theme");
    return {};
  }

  return (0, _styles.createMuiTheme)(themes[context]);
};

exports.default = createTheme;