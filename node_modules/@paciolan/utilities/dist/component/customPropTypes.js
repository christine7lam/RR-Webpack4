"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var _customPropTypes = {};

_customPropTypes.positiveInteger = function (required) {
  return function (props, propName, componentName) {
    if (typeof props[propName] !== "number") {
      return new TypeError("Invalid prop `" + propName + "` supplied to `" + componentName + "`\n> `" + props[propName] + "` is not a number.");
    }
    if (props[propName] < 0 || !Number.isInteger(props[propName])) {
      return new TypeError("Invalid prop `" + propName + "` supplied to `" + componentName + "`\n> `" + props[propName] + "` must be an integer greater than 0.");
    }
  };
};

var customPropTypes = {};
// Apply optional and required proptype checkers
for (var key in _customPropTypes) {
  customPropTypes[key] = _customPropTypes[key](false);
  customPropTypes[key].isRequired = _customPropTypes[key](true);
}

exports.default = _extends({}, customPropTypes);